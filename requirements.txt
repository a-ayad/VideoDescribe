# Core ML/AI
transformers>=4.35.0
accelerate>=0.20.0
sentencepiece>=0.1.99

# Video/Audio processing
openai-whisper>=20231117
librosa>=0.10.0
soundfile>=0.12.0  # Required by librosa for audio I/O

# Web UI (if needed for future expansion)
streamlit>=1.28.0

# Utilities
numpy>=1.24.0
tqdm>=4.65.0

# Additional dependencies for audio processing
scipy>=1.10.0  # Required by librosa
numba>=0.57.0  # Required by librosa for performance
decorator>=4.4.2  # Required by librosa
audioread>=3.0.0  # Required by librosa for reading audio files
pooch>=1.6.0  # Required by librosa for dataset management
joblib>=1.3.0  # Required by librosa
scikit-learn>=1.3.0  # Required by librosa
lazy-loader>=0.3  # Required by librosa
msgpack>=1.0.0  # Required by librosa
soxr>=0.3.0  # High-quality audio resampling (optional but recommended)

# System utilities
packaging>=23.0  # Required for version checking
